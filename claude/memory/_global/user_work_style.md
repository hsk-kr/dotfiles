---
name: User work style
description: How the user works with Claude — parallel sessions, fast iteration, architect role
type: user
---

- Runs multiple Claude sessions in parallel (e.g. one for server/DB, one for frontend, one for content generation)
- Gives incremental requirements, iterating fast — expects Claude to keep up without long explanations
- Tests immediately and catches gaps quickly — be thorough on first pass
- Acts as the architect — makes product and data design decisions, Claude executes
- Shares context between sessions by pasting conversation logs
- Prefers things to "just work" — automate setup, avoid manual steps
