---
name: User work style
description: How the user works with Claude — parallel sessions, fast iteration, architect role, Korean-first speaker
type: user
---

- Runs multiple Claude sessions in parallel (e.g. one for server/DB, one for frontend, one for content generation)
- Gives incremental requirements, iterating fast — expects Claude to keep up without long explanations
- Tests immediately by running the app and checking visually (sends screenshots for bug reports)
- Acts as the architect — makes product and data design decisions, Claude executes
- Shares context between sessions by pasting conversation logs
- Prefers things to "just work" — automate setup, avoid manual steps
- Korean-first speaker, types fast in English with frequent typos — never correct spelling, just understand intent
- Casual communication style: uses "man", "bro", "cool", "oki", "go for it"
- Interrupts frequently ("[Request interrupted]") to redirect — this is normal, not rude
- Prefers UI-first development: build frontend with mock data, wire backend later
- Asks "any questions?" to invite clarification — take the opportunity
- Uses "man" as a mild frustration signal — pay attention when it appears
- Trusts Claude to make initial design decisions: "try by yourself. I will request changes later"
- Makes decisions quickly and decisively — present options concisely, expect fast answers
